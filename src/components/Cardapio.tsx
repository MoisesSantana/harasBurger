import React from 'react';
import {
  sanduiches,
  trios,
  porcao,
  bebidas,
} from '../data/listaDeAlimentos';

const Header: React.FC = () => (
  <section>
    oi
  </section>
);

export default Header;
